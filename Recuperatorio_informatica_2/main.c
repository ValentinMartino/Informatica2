#include <stdio.h>
#include "funciones.h"



int main()
{
	ptrTaxi ptrInicial = NULL;

    printf("hola");
    //leer_archivo(&ptrInicial);
       



    //if(archivo_taxi(ptrInicial) == -1)
      //  printf("\nError no se pudo crear el archivo con los datos del taxi.\n");
	
	
	
	
	/*ptrTaxi ptrNuevo = NULL;//creamos la variable que apuntara al primer nodo
	int variable, interno;
	
	
	leer_archivo(&ptrNuevo);// se leera el archivo binario con los datos y se crearan las estructuras de datos
	
	do
	{
        	printf("\npresione 1)para agregar los datos del taxi 2)para salir de las opciones\n");
        	scanf("%i", &variable);

        	switch(variable)//el usuario tendra la opcion de agregar los datos del taxi que no fueron enviados por el archivo binario
        	{
         	   case 1: 
         	   	printf("ingrese el numero de interno que desea buscar: \n");//puede poner el numero de interno y agregarlo
         	   	scanf("%d",&interno);
         	   	agregar(&ptrNuevo, interno); break;
        	}

    	}while(variable != 2);
	
	//una vez modificado los datos se los pasara a sus archivos de texto correspondiente

	archivo_taxi(ptrNuevo);*/
	


return 0;
}
